#include <stdio.h>
#include <stdlib.h> 
#include <string.h>

#include "headers.h"

Carro * busca1entrada(Parque ** parques, Matricula matricula, Data data_anterior, Horas hora_anterior){

}

Carro ** busca_entradas(Parque ** parques, Matricula matricula) {
    int tamanho = 8;
    Carro ** entradas = (Carro**) malloc(sizeof(Carro*)*tamanho);
    Data data_anterior;
    Horas hora_anterior;

}